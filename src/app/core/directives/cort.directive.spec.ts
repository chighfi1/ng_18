import { CortDirective } from './cort.directive';

describe('CortDirective', () => {
  it('should create an instance', () => {
    const directive = new CortDirective();
    expect(directive).toBeTruthy();
  });
});
